
/*
 * Creates the event that returns the current position in our command hierarchy.
 */
create-event:hyperlang.position.get

  /*
   * Returns the current position to caller.
   */
  p5.web.viewstate.get:hyperlang.position
  return:x:/@p5.web.viewstate.get/*?value
