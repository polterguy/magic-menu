
/*
 * Returns the current position in our "grammar-tree" hierarchy.
 */
create-event:magic-menu.grammar-position.get

  /*
   * Returns the current position to caller.
   */
  p5.web.viewstate.get:magic-menu.position
  return:x:/@p5.web.viewstate.get/*/*/0/-?value
