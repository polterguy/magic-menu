
/*
 * Creates the event that closes the "help" window.
 */
create-event:magic-menu.options.close

  /*
   * Checking if widget exists already, and if so, deleting it.
   */
  if
    fetch:x:/0/0?value
      widget-exists:magic-menu-help
    delete-widget:magic-menu-help
